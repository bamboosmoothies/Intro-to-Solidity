pragma solidity >=0.5.0 <0.6.0;

contract Example {
    // This will be stored permanently in the blockchain
    uint myUnsignedInteger = 100;

    /* 
        uint - unsigned integer meaning its value must be non-negative
     */
}
